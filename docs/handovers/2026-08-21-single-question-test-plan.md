# 2026-08-21 单问题测试计划交接

## 本轮完成

- 审计并修复“原型范围 → 真实测试计划”断点；旧三问题抽屉升级为四步单问题证据计划。
- 新增 5 条来源、4 条 Claim、5 份资源审阅和第 29 个任务入口；权威计数为 542 资源、296 Claim、542 审阅、29 入口。
- 原型范围增加完整校验后的“继续到测试计划”；抽屉内导入仍必须显式触发且只复制原字段。
- 新计划覆盖预测/反驳、观察事件、重复/起止、单一变化、参与者/配置、主持边界、记录同意、安全停止、局后追问、结论规则和停车区。
- 历史继续进入项目包 `test_plans`；独立导出升级为 schema v2，并拒绝乐趣分、样本代表性、因果证明、发行认证和静默推断。
- 接受两张概念稿：`design/concepts/single-question-test-plan-drawer.png` 与 `design/concepts/single-question-test-plan-hosting.png`。

## 验证边界

类型检查、内容模型和结构守卫可以证明字段、协议与构建关系存在，不能证明真实浏览器中的焦点、滚动、下载、移动端或视觉保真，也不能代替真人形成性测试。

## 当前阻塞

Vite 在 `127.0.0.1:5175` 监听时返回 `EPERM`，页面未加载。因此本轮浏览器脚本只完成静态语法更新，未获得运行截图或交互证据。

## 下一步

恢复浏览器 QA 后，优先跑 `USABILITY_TEST_SINGLE_QUESTION_PLAN.md`。随后应实现“现场主持/逐事件记录”层：从计划生成不修改原计划的会话记录，让实际事件、帮助、停止和局后回答回接到设计决定。
