# 蛇梯棋与竞速主线迭代 · 2026-09-11

系统学习默认改为从《蛇梯棋》进入的 28 章中英文课程。保留原来的 28 章过河路线为完整替代路径。仍然是每篇一个内容版本、中文和英文两种语言。

## 内容与交付

- 新主线 ID 为 `race-*`，7 部分、28 章，正文按章懒加载；保留 13 篇选读。
- 原 `reading-*` 的 56 份正文逐字保留。旧目录原文存于 `content/reading-path-river.json`；原 28 组练习配置和示例仍完整保留。
- 默认目录 `#course/reading/all/zh-CN`；替代目录加 `?track=river`。旧章节 ID 本身识别路线，无需重定向或改旧链接。切换案例按章节序号对应；语言、目录返回和关联阅读维持上下文。
- 基础 B、选骰 C、重掷 R、补给 E、合作 S 均明确为不同实验；最终候选为两人 C。第 27 章给出完整规则，第 28 章说明交付、版本和下一次待测问题。
- 《蛇梯棋》《蒙扎赛车》《欲罢不能》《骆驼大赛》《蜗牛赛跑》五组对照，列出具体版次与出版者规则链接。《蒙扎赛车》《蜗牛赛跑》明确为暂译名，不声称找到授权中文版。
- 交互棋盘使用精确的 24 格教学规则；选 2 为 6→8→17，选 6 为 6→12。这是固定示例，不是假装发生的随机试玩。
- 可打印材料：`public/downloads/race-c-v0.1-{zh-CN,en}.html`，含离线棋盘与完整规则。由第 27 章及共享规则模型生成；`node --experimental-strip-types scripts/generate-race-handout.mjs` 可重建。
- 全部 18 个原有案例增加中文显示名与搜索别名，英文名称保留为英文身份。案例目录、详情、正文、推荐与课程关联显示相应语言名称。已有来源链接、分析数字及小节锚点保留。

## 核查

`pnpm check`、`pnpm build`（全部内容检查）、`pnpm qa:race-course` 通过。覆盖双路径每条 28 章、56 组双语练习、69 篇原创文章、18 个案例及名称搜索；144 次移动边界与两骰 36 个有序组合；打印材料与规则源一致性。文本可读性审计和全文索引已重建。

浏览器检查包括新主线首页、选 2/6 不同结果、首章正文与示例、英文同章、过河目录和对应章返回、中英文路径切换、390px 手机布局、完整规则章和打印入口。另有三项研究交叉核查修正：R 重掷必须接受新结果；《骆驼大赛》末赛段可因越线提前终止；Hasbro 转盘版超出终点不移动。

没有真人试玩结论；课程内对话、记录与实验分别标明构造、计划或待验证。规则版本事实与原创推断分开。对中文名《大领主》的核验依赖版本数据库和展会记录，未获得出版者独立产品页；没有将其标为出版社直接核验。

本轮不引入新依赖、订阅或付费 API。沿用现有 React 与 UI 组件。本地预览与源项目同步；本次没有额外提交、推送或生产部署操作。
